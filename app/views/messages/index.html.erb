<div class="wrapper">
  <div class="side-ber">
  <div class="side-ber-header">
  <div class="header-name">
   <%= link_to current_user.name,new_room_path(current_user) %>
   <link rel="stylesheets" href="room/room.css">
  </div> 
  <div class="create-rooms">
        <%= link_to "チャットを作成する", new_room_path %>
  </div>
</div>

  <div class="rooms">
  <% current_user.rooms.each do |room| %>
   <div class="room">
     <div class="room-name">
      <%= link_to room.name,room_messages_path(room) %>
    </div>
  </div>
 <% end %>
</div>
  </div>
  <div class="chat">
  <%= render "main_chat"%>
  </div>
</div>


<%#=チャットルームのボタンを押すとチャットを作成するのページにいく＝＞新規チャットルームのページ%>